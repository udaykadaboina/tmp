<%= form_for(@counter) do |f| %>
  <% if @counter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@counter.errors.count, "error") %> prohibited this counter from being saved:</h2>

      <ul>
      <% @counter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :goal %><br />
    <%= f.number_field :goal %>
    <%= f.submit "upG", :action => "update_goal" %>
  </div>
  <div class="field">
    <%= f.label :foul %><br />
    <%= f.number_field :foul %>
    <%= f.submit "upF", :action => "update_foul" %>
  </div>
 <!-- <div class="actions">
    <%= f.submit %>
  </div> -->
<% end %>
