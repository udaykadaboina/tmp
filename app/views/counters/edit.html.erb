<h1>Editing counter</h1>

<%= render 'form' %>
<!-- <%= form_for(@counter) do |f| %>

 <div class="field">
  <%= f.label :goal %><br />
  <%= f.number_field :goal %>
</div>
<div class="actions">
    <%= f.submit  'update_goal'%>
  </div>
<% end %>
<%= form_for(@counter) do |g| %>
<div class="field">
    <%= g.label :foul %><br />
    <%= g.number_field :foul %>
  </div>
 <div class="actions">
    <%= g.submit 'update_foul'%>
  </div> 

  
<% end %>
-->
<%= link_to 'Show', @counter %> |
<%= link_to 'Back', counters_path %>
